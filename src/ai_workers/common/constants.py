"""Shared constants for AI workers."""

from __future__ import annotations

# Modal app configuration
SCALEDOWN_WINDOW = 300  # Time in seconds to wait before scaling down a worker
KEEP_WARM = 0  # Number of containers to keep warm when idle
